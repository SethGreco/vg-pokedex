version: "3"

services:
  fast-api:
    build: .
    ports: 
      - "8000:8000"
    volumes:
      - ".:/app"
    environment:
      - MODULE_NAME=main
    healthcheck:
      test: "curl -f http://localhost:8000/api/v1/health"
      interval: 30s
      timeout: 10s
      retries: 2
    

